{
  "version": 2,
  "builds": [
    {
      "src": "server.js",   // or index.js — match this to your entry file
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"  // again, match the entry point
    }
  ]
}
